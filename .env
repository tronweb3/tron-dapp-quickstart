# Public defaults (safe to commit). Override locally in .env.local (git-ignored).
TDQ_FULL_HOST="https://nile.trongrid.io"
TDQ_CURRENT_CHAIN="Nile Testnet"
TDQ_EXPLORER_BASE="https://nile.tronscan.org"

# Optional: TronGrid API key. Strongly recommended to avoid rate limits.
# Put your real key in .env.local, NOT here. Get one at https://www.trongrid.io
# TDQ_API_KEY=""
